# Uncomment the next line to define a global platform for your project
platform :ios, '17.0'

target 'DHLModalHelper' do
  #use_frameworks!

  # Pods for DHLModalHelper

end

ENV["COCOAPODS_DISABLE_STATS"] = "true"